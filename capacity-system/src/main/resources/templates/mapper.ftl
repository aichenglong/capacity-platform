package ${basePackage}.${model}.mapper;

import com.capacity.platform.common.mapper.BaseMapper;
import ${basePackage}.${model}.entity.${modelNameUpperCamel};

/**
 * Author: ${author}
 * Date:${date}
 * Description:
 * Created by ${author} on ${date}.
 */
public interface ${modelNameUpperCamel}Mapper extends BaseMapper<${modelNameUpperCamel},${id}> {

}
